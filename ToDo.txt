流式LLM和TTS：已完成
做新语音：暂时完成（用原神英配拼着训的SoVITS，配合neuro-evil的GPT）
skill / mcp / toolcall：框架已完成，目前支持：查天气（假）、给sister发消息（websocket）、查日记。


当前内容：

基础API调用（LLM_basic）：
 - 管理多种模型的api client。支持doubao和deepseek的流式调用。

上下文工程（LLM_context）：
 - 模块化上下文：支持系统提示词、记忆模块、对话多级缓冲队列、中期提示词注入等模块的自由拼接。
 - user_input 处理：RAG部分+[时间标签]+[身份标签] + [user_input_text]。其中RAG部分是临时的，不会进入上下文历史中。

中短期记忆（上下文）：
 - 三级缓冲结构：一级缓冲存对话，二三级存压缩后文本，缓冲满后压缩内容并清空、逐级向上push。顶层缓冲满后对提示词中的所谓长期记忆部分（prompt_dict["memory"]）进行更新。三级缓冲理论上最多能存下15*15*10=2250轮对话信息，长期记忆那部分则是永远保持更新的总结性记忆。

长期记忆（持久化存储）：
 - 日记系统：分为文本模块（./saved_context/{IDENTITY}/diary/）和数据库模块（./vector_db/{IDENTITY}/diary/）。文本模块按日周月年四个jsonl存储；数据库模块统一存储、根据day/week/month/year的日期前缀进行区分。由自动化脚本（./diary/auto_trigger.py）在每天0点自动触发更新，调用日记提示词和人格模块让IDENTITY角色自己书写。

MCP：
 - chat_sister: 给另一台机器上运行的 mylibra 发消息。
 - search_diary: 按时间范围查日记。范围以日为单位，跨周月年的区间会被自动合并成对应的周记/月记/年记。最后返回拼接了所有查询结果的字符串。

TTS（TTS.py; [mylibra-TTS]）：
 - 给GPT-SoVITS做了个推理调用的接口，并能够运行在本地WebSocket服务器（asyncio）上，能接受文本请求，并按照指定参数（指定参考语音）返回TTS音频。
 - 目前使用的语音：自己训的SoVITS模型 + b站有人分享的neuro/evil的GPT模型。参考语音用的neuro双子和原神英配（Ineffa, Lanyan, Lynett）的融合版。

服务端（server.py）：
 - 需要公网ip。目前用的cpolar。
 - 与客户端（比如Android）建立WebSocket连接，接收文本数据（user-input），响应llm回复的文本和tts的音频。
 - 支持流式响应：实时切句，转TTS，再发送[文本-语音]分片。

客户端（[Mylibra_App_v1]）：
 - 本地STT：百度API。
 - 发送文本消息，处理响应文本和音频。适配了服务端的流式响应，用有序字典和音频队列实现音频播放的保序性。

-LLM：实现对话的核心功能。其余功能被拆分到LLM_basic（API管理）和LLM_context（上下文管理）中。
-Vdb：faiss数据库的实现。
-TTS：这个功能被抽离到子项目[mylibra-TTS]中实现了，主项目里它只负责调用。
-STT：本地的语音识别。支持麦克风实时监听。*理想中还应该有个buffer，如果在LLM回复过程中又有了新的输入，就先存进buffer里，等回复结束后再发过去。
-STT_LLM_TTS：本机的总流程，以及向 server.py 提供的接口类。
-server：服务端实现。基于WebSocket。—— 目前的API-KEY还暴露在请求url中，后面找机会改掉
-[./diary/]: 自动写日记的监控程序，0点自动更新
-[mylibra-TTS]：负责TTS的子项目。目前仅支持GPT-SoVITS的本地推理，如果以后要用其他模型的话，可能还得再写个单独的。把它抽离出来本身就是为了简化环境配置、避免这个项目和GPTSoVITS的项目杂糅。
-[Mylibra_App_v1]：安卓客户端。用于远程通讯。





下一步：
---- 项目：
开源准备：把 global config 和 prompt update 整合到一块，做一个统一设置的前端。注意藏好隐私信息。（done）
统一输出日志。把print全改成log。
做个 Web 客户端，这样就能在 PC 上访问了。（done）


---- 人格化方面：
***加入知识图谱。现在细节记忆的能力还是太差了，知识图谱可以针对细节记忆做一个补全。
加入自动上网冲浪的功能。可以每天自动触发，或者通过mcp自主触发。冲浪获取到的内容作为单独一轮或几轮对话正常插入上下文。冲浪的内容可以是爬取新闻或逛逛社区？
加入情绪标签和情绪模拟（就像myneuro那样）。可以先输出固定的几个情绪标签，TTS那边根据标签选择不同的模型和参考音频。
加入自动阅读壁纸的功能？

***加入世界书。
一阶段：世界状态存储（世界层-可共享；个体层-私有），世界时钟（自动状态更新程序）
世界层：基础设定（和现实世界一样？），时间（与现实时间同步），地点，环境状态（天气），编年史（大型事件的记录表）
个体层：基础设定（静态，可能不定期更新。身世，性格，外貌），状态（实时。生理和心理。生病，失眠，亢奋，开心等），社会位置（动态。工作，住哪，人际关系（是否所有人物都要引入llm角色？可否简化（群像不需要太精细的模拟）？））
世界时钟：写好结构化世界框架后，再规定一套Action让框架动起来。

二阶段：世界与 Agent 的通信（交互）
让 Agent 意识到自己身处世界中。
如何制定交互规范？

三阶段：多 Agent 在模拟世界中的交互
应该属于二阶段的扩展。还没想好。


---- Agent能力方面：
读日记的mcp现在非常拉。找不到大区间的时候应该降级去小区间里找，比如week找不到就降级到day，month找不到降级到week，逐级递归。
网络搜索、关电脑
单词本。单独一个数据库或什么的存储单词本，让ai在聊天时随时访问并提问，把增删改查的功能接口开放给ai。--思路打开一些，也可以不仅仅是单词本，随便记点什么笔记都可以。这个笔记最好是我们共享的，我也可以方便地在里面写一些东西让ai看。

多智能体协作：
-- 暂定


---- 其他：
更新下Android客户端的界面。还有对发送的上下文也进行加解密，还有把暴露在url里的API-KEY藏进去。





---- Till
26-04-01：
增加了上下文记忆的三级缓冲队列和中期提示词注入。
完成了日记系统的写入（日级别已正常运行）和 MCP 查询。
重构了 LLM 的结构，把基础API调用拆分到 LLM_basic 里，把上下文管理拆分到 LLM_context 里。

26-04-02：
限制 toolcall 连续调用次数为 1 次。
加入 chatroom, 添加做梦功能（情景演绎，效果很差）和姐妹聊天功能（让Libra和Yunru自行对话，由程序控制对话轮数）

26-04-04:
上午：把 global_config 的设置独立到文件里（./configs/saved_configs/config_global.json），为开源做好隐私保护和配置透明度的准备。新增控制台使用命令的权限（在受限隔离环境下）。

